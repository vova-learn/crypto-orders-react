export const getTicker = (state) => state.ticker;
export const getTickers = (state) => state.tickers;
export const getOrderbook = (state) => state.orderbook;
export const getLoadOrderbookStatus = (state) => state.isLoadOrderbook;
